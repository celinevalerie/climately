<div class="header navbar-light navbar-lewagon">
  <%= link_to root_path, class: "navbar-brand" do %>
    <%= cl_image_tag("no-straws-white_esdvml") %>
  <% end %>

  <h2 class="white mb-0"> <%= @navbartitle ? @navbartitle : "" %> </h2>
    <div class="button-mobile-secondary">
      <% if user_signed_in? %>
        <%= link_to "Log out", destroy_user_session_path, method: :delete %>
      <% else %>
        <%= link_to "Log in", new_user_session_path %>
      <% end %>
    </div>
</div>
 
<%# to change the title on the phone go to the controller of the page and put @navbartitle = "what we want" %>